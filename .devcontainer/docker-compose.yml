version: '3'

services:
  app:
    build: 
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ~/.ssh:/opt/yijun/.ssh
      - ..:/opt/yijun/code
    command: ["tail -f /dev/null"]
    ports:
      - 8123:8000
    depends_on:
      - db-for-user-backend



